
<nav class="navbar navbar-default">
  <div class="container-fluid">  <!-- better mobile display -->
    <div class="navbar-header">     
      <a class="navbar-brand" href="<%= home_path %>"> <%= image_tag "gift-box.png", align: "left", size: "25x25", alt: "Want-It", style: "padding-right: 15px; padding-top: 15px; padding-left: 15px;" %> </a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->

    <div class="collapse navbar-collapse" id="something">
      <ul class="nav navbar-new">
        <% if session[:user_id].nil? %>
          <a href="<%= login_path %>"> LOG IN </a>
        <% else %>
          <a><%= @current_user.user_name %></a>
          <a><%= link_to "LOG OUT", logout_path, method: :delete %></a>
        <% end %>
      </ul>    
    </div>

  </div>
</nav>
