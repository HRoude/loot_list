
<nav class="navbar navbar-default">
  <div class="container-fluid">  <!-- better mobile display -->
    <div class="navbar-header">     
      <%= link_to '<span class="glyphicon glyphicon-gift"></span> HOME'.html_safe, home_path, method: :get, class: 'btn btn-default btn-md' %> 
        <% if session[:user_id] %>
          <%= @current_user.name %> is logged in   
        <% end %><br><br> 

        <% if session[:user_id] %>
          <%= link_to "YOUR PROFILE", user_path(@current_user), class: 'btn btn-default btn-md' %>
        <% end %>  
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->

    <div class="collapse navbar-collapse" id="something">
      <ul class="nav navbar-new"><br><br>
        <% if session[:user_id].nil? %>
          <a><%= link_to "LOG IN", login_path, class: 'btn btn-default btn-md pull-right' %></a>
        <% else %>
          <a><%= link_to "LOG OUT", logout_path, method: :delete, class: 'btn btn-default btn-md pull-right' %></a><br><br>
        <% end %>
      </ul>

      <ul>
        <% link_to "USER PROFILE", user_path(@current_user) if session[:user_id] %>
      </ul>  
    </div>

  </div>
</nav>
